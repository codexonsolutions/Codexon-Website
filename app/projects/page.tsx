import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Portfolio & Success Stories",
    description: "Explore our past projects and see how we've helped businesses achieve digital success.",
};

export default function ProjectsPage() {
    return (
        <div>
            <h1>Projects Page</h1>
        </div>
    );
}